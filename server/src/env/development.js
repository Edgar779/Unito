import * as path from 'path';

const config = {
  NODE_ENV: 'development',
  PORT: 3000,
  MONGO_URL: "mongodb://localhost:27017/Unito",
  JWT_SECRET: 's%4^3paraSA5Ddox8A$D!#',
};

export default config;